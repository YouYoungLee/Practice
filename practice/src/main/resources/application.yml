server:
  port : 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pracdb?useSSL=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: you*#789789
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
#        show_sql: true
    defer-datasource-initialization: true


